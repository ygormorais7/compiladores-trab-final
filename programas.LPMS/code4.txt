Program_Fatorial {
    int fat;
    int res;

    res = 1;

    input(fat);

    while( fat > 1 ){
        print (res);
        res = res * (fat - 1);
    }

    print("Resultado final:");
    print(res);
}